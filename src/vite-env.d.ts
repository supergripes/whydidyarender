/// <reference types="vite/client" />

// Injected by vite.config.ts's `define`; true unless this build was
// started with WHYDIDYARENDER_COMPILER=off.
declare const __REACT_COMPILER_ENABLED__: boolean;
